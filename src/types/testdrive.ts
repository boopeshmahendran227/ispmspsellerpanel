export interface TestDriveInterface {
  customerId: string;
  customerName: string;
  customerPhone: string;
  images: string[];
  productId: number;
  productName: string;
  sellerId: string;
  skuId: string;
  requestedDate: string;
}
